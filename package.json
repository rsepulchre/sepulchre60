{
  "name": "sepulchre60",
  "private": true,
  "scripts": {
    "build": "node scripts/render.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
